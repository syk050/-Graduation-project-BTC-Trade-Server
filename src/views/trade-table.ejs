<table>
    <thead>
        <tr>
            <th>date</th>
            <th>price</th>
            <th>amount</th>
            <th>total_amount</th>
        </tr>
    </thead>
    <tbody>
        <% list.forEach(function (item){ %>
            <tr>
                <td><%= item['date'] %></td>
                <td><%= item['price'] %></td>
                <td><%= item['amount'] %></td>
                <td><%= item['total_amount'] %></td>
            </tr>
        <% }); %>
    </tbody>
</table>